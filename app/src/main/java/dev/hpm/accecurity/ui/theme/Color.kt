package dev.hpm.accecurity.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// COLORES BASE - TEMA CLARO

// Color Principal (Botones de crear pase, iconos seleccionados)
val Primary = Color(0xFF3B628F)
val OnPrimary = Color(0xFFFFFFFF)

// Contenedor Principal (Filtros seleccionados en el Home)
val PrimaryContainer = Color(0xFFD3E0F5)
val OnPrimaryContainer = Color(0xFF3B628F)

// Color Secundario (Botón de Iniciar Sesión en Login)
val Secondary = Color(0xFFA6B7C9)
val OnSecondary = Color(0xFFFFFFFF)

// Contenedor Secundario (Botón de '+' dentro del botón de crear pase)
val SecondaryContainer = Color(0xFF5A7DA3)
val OnSecondaryContainer = Color(0xFFFFFFFF)

// Fondos
val Background = Color(0xFFF8F9FA) // Fondo general del Home
val OnBackground = Color(0xFF1A1C1E) // Textos principales (negro/gris oscuro)

val Surface = Color(0xFFFFFFFF) // Fondos de tarjetas, barra inferior, items
val OnSurface = Color(0xFF1A1C1E)

// Variantes de Superficie y Bordes
val SurfaceVariant = Color(0xFFEFEFEF) // Cuadro de estado "Salieron" o fondos grises
val OnSurfaceVariant = Color(0xFF6B7280) // Textos secundarios (subtítulos grises)
val Outline = Color(0xFFD1D5DB) // Bordes de TextFields y tarjetas
val OutlineVariant = Color(0xFFE5E7EB) // Bordes más sutiles

// COLORES DE ESTADOS ESPECÍFICOS (Custom)

val StatusExpectedContainer = Color(0xFFFFE099)
val StatusExpected = Color(0xFFD69E2E)

val StatusInsideContainer = Color(0xFFA5E6AC)
val StatusInside = Color(0xFF38A169)

// COLORES TEMA OSCURO (Opcional a futuro)
val PrimaryDark = Color(0xFF8FB3E0)
val SecondaryDark = Color(0xFFBCCCDC)